package coreJava.advanced.encapsulation;

public class Encapsulation {

    // Encapsulation: A mechanism of wrapping the data(variables) and code acting on the data(methods) together as single unit.
    // It describes the ability of an object to hide its data and methods from the rest of the world.
    // Class and objects are the examples of wrapping the data (variables and methods) in a single unit.
    // If any component follows data hiding and abstractions is said to be encapsulated.

    String name;
    String city;
    private int mobileNumber;
    private int bankBalance;


    public Encapsulation(String name){
        this.name = name;
    }


    public static void main(String[] args) {

    }
}
