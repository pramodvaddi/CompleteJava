package coreJava.intermediate;

public class ForEach {
    /*
    * For Each Loop: This loop is also known as extended for loop.
    * For Each loop is only used for Arrays and collections.
    * In ForEach, we don't need to increment and mention the size of an array.
    * ForEach can only increment values, but not decrement.
    * ForEach is not suitable for index.
    */

    public static void main(String[] args) {

    }
}
